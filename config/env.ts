// Environment Configuration
const ENV = {
  API_BASE_URL: '', // Android emulator
  // For iOS/Physical device, use: 'http://localhost:5000/api'
  // For production, use your actual backend URL
};

export default ENV;
